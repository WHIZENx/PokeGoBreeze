/* eslint-disable no-unused-vars */
export enum RaidState {
  WIN = 'WIN',
  LOSS = 'LOSS',
  TIMEOUT = 'TIME OUT',
}

export enum SortType {
  DPS,
  TDO,
  TANK,
}

export enum SortDirectionType {
  ASC,
  DESC,
}
