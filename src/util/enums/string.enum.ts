/* eslint-disable no-unused-vars */
export enum EqualMode {
  CaseSensitive = 1,
  IgnoreCaseSensitive,
}

export enum IncludeMode {
  Include = 1,
  IncludeIgnoreCaseSensitive,
  IncludeBetween,
  IncludeBetweenIgnoreCaseSensitive,
}
